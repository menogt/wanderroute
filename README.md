
  # Travel plan app

  This is a code bundle for Travel plan app. The original project is available at https://www.figma.com/design/sdVgeEVKTLGDywysWiRbp1/Travel-plan-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  